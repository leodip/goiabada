// Package accounthandlers serves self-registration: the registration page, and the activation
// link it emails when the settings require a verified address. Both are browser pages the auth
// server renders; the account self-service API a signed-in user calls is apihandlers'.
package accounthandlers
